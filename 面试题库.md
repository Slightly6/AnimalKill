# Unity 秋招面试题库

> 目标是"必考高频不翻车 + 项目能讲出彩"。先啃必死题，再刷算法，最后包装项目。

---

## 一、10 天复习计划

| 天 | 内容 | 目标 |
|---|---|---|
| D1 | C# 值类型/引用类型、装箱拆箱、struct vs class、string | 能白板讲清楚 |
| D2 | GC、委托/事件、协程 vs 线程、List/Dictionary 底层 | 协程必背 |
| D3 | Unity 生命周期、Update/FixedUpdate、Quaternion、触发 vs 碰撞、射线 | 生命周期背到顺口溜 |
| D4 | 设计模式：单例、观察者、对象池、状态机（拿项目举例） | 每个能说 3 分钟 |
| D5 | 算法：链表、栈/队列、哈希、二叉树遍历 | 手写基本操作 |
| D6 | 算法：二分、双指针、快排/归并、简单 DP | 每题 15 分钟内 |
| D7 | Unity 进阶：Canvas 三模式、动画状态机、ScriptableObject、Draw Call、合批、Profiler | 知道"是什么"即可 |
| D8 | 项目复盘：AnimalKill 架构 + 踩坑整理成口述 | 简历 + 能讲 |
| D9 | 模拟面试：高频题，答 + 点评 | 查漏补缺 |
| D10 | 冲刺：过一遍必死题 + 面试技巧（自我介绍、反问） | 稳心态 |

> 算法别只堆在 D5-D6，每天抽 40 分钟刷 1~2 题保持手感。

### 10 天内直接砍掉（别碰）
- 图形学 shader 深入、渲染管线细节（知道 Draw Call 是啥就够）
- 热更新（xLua/HybridCLR）、DOTS/ECS（除非岗位明确要）
- 网络协议深入（除非投联网游戏）
- LeetCode 困难题

---

## 二、C# 基础（占比最高）

### 1. 值类型 vs 引用类型（必背）
- **值类型**：`int`、`float`、`bool`、`struct`、`enum`，存在**栈**上，赋值是**复制一份**
- **引用类型**：`class`、`string`、`数组`、`delegate`，存在**堆**上，栈里存的是**地址**，赋值是**传地址**
- 追问：`string` 是引用类型，但表现像值类型（因为不可变）

### 2. 装箱/拆箱（必背）
- **装箱**：值类型 → `object` 或接口，会在堆上复制一份（有开销）
- **拆箱**：`object` → 值类型，强制转换
- 例：`object o = 5;` 是装箱；`ArrayList.Add(5)` 每个都装箱（所以有了泛型 `List<int>` 避免装箱）

### 3. GC 原理
- 托管堆分**三代**（0/1/2），新生对象在 0 代，GC 用**标记-清除**回收没被引用的对象
- 会**卡顿**（stop the world），所以 Unity 里要**少 new、少字符串拼接、用对象池**
- 优化：`StringBuilder`、对象池、`struct`、避免装箱

### 4. struct vs class
- struct 是值类型、class 是引用类型
- struct 不能继承（但能实现接口）、无默认无参构造函数、栈上分配

### 5. 委托、事件、泛型
- `delegate` 是"指向方法的类型"，`event` 是受限的委托（外部只能 `+=`/`-=`，不能直接赋值/调用）
- `Action`（无返回值）、`Func`（有返回值）、`Predicate`（返回 bool）
- 泛型：编译时确定类型，避免装箱，类型安全
- 坑：委托 `+=` 订阅后忘记 `-=` 会**内存泄漏**（EventBus 里 `OnDestroy` 里 `Unsubscribe` 就是防这个）

### 6. 协程 vs 线程（必背）
- **协程不是多线程**，跑在**主线程**上，靠 `yield` 让出执行权，下一帧继续
- `yield return null` = 等一帧；`WaitForSeconds(1)` = 等 1 秒；`WaitForEndOfFrame` = 等帧末
- 线程是操作系统级，真正并行，但 Unity API 大多**只能在主线程调**

### 7. 接口 vs 抽象类
- 接口：只能定义方法签名，可**多实现**，强调"能做什么"
- 抽象类：可有实现和字段，**单继承**，强调"是什么"

### 8. List / Dictionary 底层
- `List` 底层是数组，满了会**扩容**（默认 4，翻倍），扩容有拷贝开销，能预估容量就 `new List<T>(capacity)`
- `Dictionary` 底层是**哈希表**，`key` 通过 `GetHashCode` 找桶，冲突用链表；查找 O(1)

### 9. 多态、重载、重写
- **重载**：同名方法、参数不同（编译期）
- **重写**：`virtual` + `override`，子类覆盖父类（运行期）
- **多态**：父类引用指向子类对象，调用时走子类实现

### 10. 其他高频小点
- `ref`（传引用，先初始化）vs `out`（传引用，方法内必须赋值）
- `static`（属于类）、`const`（编译期常量）、`readonly`（运行期只读）
- **深拷贝 vs 浅拷贝**：浅拷贝只复制引用，深拷贝复制整个对象
- `yield` 迭代器：`IEnumerable` 懒加载
- 反射：运行期获取类型信息（`GetType()`、`typeof`、`Activator.CreateInstance`）
- `try/catch/finally`，`finally` 一定执行

---

## 三、Unity 核心

### 1. 生命周期顺序（必背，一字不差）
```
Awake → OnEnable → Start → FixedUpdate → Update → LateUpdate → OnDisable → OnDestroy
```
- `Awake`：对象创建时调一次，用于初始化自己的引用
- `Start`：第一帧前调一次，用于依赖其他对象已 Awake 完
- `OnEnable`：每次激活时调
- `FixedUpdate`：固定时间步长（默认 0.02s），**物理**相关
- `Update`：每帧调，游戏逻辑
- `LateUpdate`：Update 之后调，**摄像机跟随**常用

### 2. Update / FixedUpdate / LateUpdate
- `Update`：每帧，帧率不稳时次数不稳
- `FixedUpdate`：固定频率，和物理同步
- `LateUpdate`：一帧最后，适合跟着别人动的对象（相机）

### 3. 四元数 Quaternion（必背）
- 用 `Quaternion` 表示旋转，**避免欧拉角的"万向锁"**
- `Quaternion.Euler(x,y,z)` 和欧拉角互转
- `Quaternion.Lerp` / `Slerp` 插值
- `transform.rotation` 是世界旋转，`localRotation` 是本地旋转

### 4. 碰撞 vs 触发（必背）
- **碰撞**：`OnCollisionEnter/Stay/Exit`，双方都有 Collider，至少一方有 Rigidbody
- **触发**：勾选 `isTrigger`，用 `OnTriggerEnter/Stay/Exit`
- 区别：触发不产生物理阻挡，只通知

### 5. 射线 Raycast
- `Physics.Raycast(起点, 方向, out hit, 距离, layerMask)`
- 用于点击检测、视线判断、拾取物体

### 6. Canvas 三种渲染模式
- **Overlay**：屏幕空间覆盖，永远在屏幕最上层（UI 常用）
- **Camera**：屏幕空间但受相机影响
- **WorldSpace**：世界空间，3D 里的 UI

### 7. UI 适配
- `CanvasScaler`：`Scale With Screen Size` + 参考分辨率，不同屏幕等比缩放
- 锚点 `Anchor`：相对父节点定位

### 8. Animator 动画状态机
- `AnimatorController`、状态、过渡条件（参数 `bool/int/float/trigger`）
- 混合树、`SetTrigger` 切换

### 9. ScriptableObject（项目在用）
- 数据容器，存配置/数据（卡牌数据 CardDataSO 就是）
- 优点：和场景解耦、可复用、省内存

### 10. Draw Call / 合批（性能必考）
- `Draw Call`：CPU 提交给 GPU 的一次绘制命令，**越多越卡**
- **合批**减少 Draw Call：静态批处理（Static 勾选）、动态批处理（顶点 < 900）、GPU Instancing
- 用 `Profiler` 看 Draw Call 和 GC

### 11. 资源加载方式
- `Resources.Load`：简单但都打进包、不能热更
- `AssetBundle`：可分包、可热更
- `Addressables`：官方推荐，异步加载 + 引用计数管理

### 12. 对象池（高频 + 能讲项目）
- 预先创建一批对象，用完回收不销毁，避免频繁 Instantiate/Destroy 造成 GC 卡顿
- 场景：子弹、卡牌、敌人

### 13. 其他高频小点
- `Time.deltaTime`：上一帧到这一帧的时间，用它乘速度保证帧率无关
- `NullReferenceException`：空引用，最常见 bug
- `Destroy`（延迟销毁）vs `DestroyImmediate`（编辑器用）
- `Camera.main`、渲染顺序 `SortingOrder`、`SortingGroup`
- `Layer` 和 `LayerMask`（物理/渲染分层）

---

## 四、算法与数据结构（力扣重点）

### 必会数据结构操作
- 数组：遍历、双指针
- 链表：反转、快慢指针、合并
- 栈/队列：括号匹配、BFS
- 哈希表：两数之和、去重、计数
- 树：前中后序、层序遍历、递归
- 图：BFS/DFS

### 必背代码模板
1. 二分查找
2. 快排 / 归并排序（手写）
3. DFS（递归）和 BFS（队列）
4. 滑动窗口
5. 双指针

### 力扣经典题号（按题型）

| 题型 | 题号 | 名字 |
|---|---|---|
| 哈希 | 1 | 两数之和 |
| 哈希 | 217 / 242 / 49 | 存在重复 / 字母异位词 / 异位词分组 |
| 双指针 | 283 / 11 / 15 | 移动零 / 盛水 / 三数之和 |
| 滑动窗口 | 3 | 无重复最长子串 |
| 栈 | 20 / 155 | 有效括号 / 最小栈 |
| 链表 | 206 / 21 / 141 / 160 | 反转 / 合并 / 环形 / 相交 |
| 二分 | 704 / 35 / 69 / 33 | 二分查找 / 插入位置 / x平方根 / 搜索旋转 |
| 树 | 94 / 144 / 145 / 102 / 104 / 226 / 110 | 前中后序 / 层序 / 深度 / 翻转 / 平衡 |
| DP | 70 / 121 / 198 / 53 / 300 | 爬楼梯 / 买股票 / 打家劫舍 / 最大子数组 / 最长递增子序列 |
| 回溯 | 46 / 78 / 22 / 39 | 全排列 / 子集 / 括号生成 / 组合总和 |
| 图 | 200 / 207 | 岛屿数量 / 课程表 |

> 优先级：哈希、双指针、栈、链表、二分、树遍历最常考，DP 会简单题就行。

---

## 五、设计模式（结合项目）

| 模式 | 一句话 | 项目里的例子 |
|---|---|---|
| 单例 | 一个类只有一个实例 | Singleton<T> 基类 |
| 观察者 | 发布-订阅解耦 | EventBus |
| 对象池 | 复用对象少 GC | 卡牌/子弹 |
| 状态机 | 状态切换 | 战斗回合阶段 |
| 工厂 | 统一创建对象 | 宝箱随机生成卡牌 |

---

## 六、项目深挖高频追问（提前准备）

1. 你的项目架构是怎样的？（Singleton + EventBus + ScriptableObject + GameProgress 跨场景）
2. 遇到最难/印象最深的 bug 是什么？（卡牌正反面：Quad 朝向、旋转约定、覆盖优先级）
3. 为什么用 ScriptableObject 存数据而不是 Excel/Json？
4. 怎么做性能优化的？（对象池、Draw Call、少 GC）
5. 数据怎么存档？（GameProgress 静态类 + 跨场景）

---

## 七、计算机基础（会一点就够）

- 操作系统：进程 vs 线程、堆栈区别（和 C# 呼应）
- 网络：TCP vs UDP、三次握手、HTTP 状态码（200/404/500）——投联网游戏才重点看
- 图形学：渲染管线（顶点→片元着色器）、Draw Call、坐标系（世界/屏幕/相机）
