# 背包系统说明（照这个学）

这个背包系统做了 6 件事：**按 Tab 开背包 → 看到拥有的武器 → 点一把装备 → 模型换掉 → Boss 死了掉武器 → 弹提示**。

## 一、整体结构（谁管什么）

```
WeaponData（数据）        = 一把武器的「说明书」：图标、名字、伤害、类型、3D 模型
     ↓ 被引用
InventoryManager（背包）  = 管「拥有哪些武器」+「点谁装谁」
     ↓ 调用
WeaponManager（装备）     = 管「3 个槽位 + 拿在手上/收在背上」
     ↓ 被引用
InventoryUI（界面）       = 管「按 Tab 开关 + 生成格子」
     ↓ 实例化
WeaponSlot（一个格子）    = 显示图标名字，点了就装备
```

辅助两个：
- `WeaponGetToast`：打死 Boss 后弹「获得狼牙锤」
- `EventBus`：这些文件之间发消息，不用互相拖引用

## 二、每个文件干什么（重点）

### 1. WeaponData —— 武器说明书
右键 `Project → Create → 武器 → 新武器`，生成一个 `.asset` 文件。
填：图标、名字、类型（近战 Melee / 远程 Ranged）、3D 模型、伤害、攻速、攻击距离、击退。
**加武器不用改代码，全靠这张表。**

#### 挂载偏移（三个字段，专门调「模型拿在手上的位置」）

不同模型的尺寸/朝向不一样（中刀 prefab 里缩放 4.46，狼牙棒只有 0.028），挂到手上会又小又歪。所以在 WeaponData 里加了三个字段，专门修这个：

- `挂载偏移 Mount Offset`（位置）：模型挂上去后相对手/背节点多移动多少。填 `(0.2, 0, 0.1)` 就是往右、往前挪一点。
- `挂载旋转 Mount Rotation`（角度，欧拉角）：模型挂上去后多转多少度。填 `(0, 90, 0)` 就是绕着竖轴转 90°。
- `挂载缩放 Mount Scale`（倍率）：在模型原本大小的基础上再乘多少。默认 1 = 保持原大小；狼牙棒原大小 0.028 太小，填 160 左右就和中刀差不多大。

**调法**：在 Inspector 里一边改这三个数、一边看 Scene 里模型的位置，改到满意为止。每把武器单独调，互不影响，不用改代码、不用改预制体。

### 2. WeaponManager —— 拿在手上（挂在 Hero 上）
3 个固定槽位：
- 槽 0 = 近战（刀）
- 槽 1 = 远程（弓）
- 槽 2 = 空着（以后用）

它负责：从 WeaponData 里 `Instantiate` 出模型、挂到手上（handNode）/背上（backNode）、拔刀收刀。

### 3. InventoryManager —— 拥有哪些（挂在单独空物体上）
- `weaponPool`：所有武器都拖进来（一个数组）。
- `ownedIndices`：记录你拥有池里哪几个下标。
- `EquipToSlot`：看你点的武器是近战还是远程，自动装到槽 0 或槽 1。
- `OnBossDied`：收到 Boss 死亡事件 → 把掉落武器加进背包。

### 4. InventoryUI —— 界面（挂在 Canvas 的 InventoryPanel 上）
按 Tab 开/关；打开时遍历 `GetOwnedIndices()`，每把武器生成一个格子。

### 5. WeaponSlot —— 一个格子（挂在格子预制体上）
`Setup()` 填图标/名字/伤害；点它调 `EquipToSlot`。已装备的格子背景变金色。

## 三、一条数据流走一遍（打死 Boss → 换武器）

1. Boss 死 → `SquirrelHp` 发 `DiedEvent`（isPlayer=false）
2. `InventoryManager.OnBossDied` 收到 → `AddWeapon(狼牙锤下标)`
3. `AddWeapon` 发 `WeaponGetEvent` → `WeaponGetToast` 弹「获得狼牙锤」
4. 按 Tab → `InventoryUI.Toggle` 打开 → `Refresh` 生成格子（中刀 + 狼牙锤）
5. 点狼牙锤 → `WeaponSlot.OnClick` → `EquipToSlot(1)`
6. 发现狼牙锤是近战 → `weaponManager.SetSlotData(0, 狼牙锤)` → 装进槽 0 → 切模型 → 拔刀

## 四、怎么加一把新武器（练手）

1. 右键 `Create → 武器 → 新武器`，填：图标、名字「大砍刀」、类型 `Melee`、模型、伤害 20。
2. 把新武器拖进 `InventoryManager` 的 `weaponPool`（数组 +1）。
3. 想让 Boss 掉它，把 `bossDropWeaponIndex` 改成它的下标。
4. 完事，不用碰代码。

## 五、怎么验证（跑一遍）

1. 进游戏按 `Tab`，背包面板出现，显示中刀。
2. 点中刀格子，确认装备上（模型切换）。
3. 打死 Boss → 弹「获得狼牙锤」→ 再按 Tab → 多出狼牙锤格子 → 点它 → 换武器。

## 六、常见坑

| 现象 | 原因 | 解决 |
|---|---|---|
| 按 Tab 没反应 | InventoryUI 挂在了会被隐藏的面板上，物体隐藏后 Update 停 | 把 InventoryUI 挂到 Canvas（常驻物体）上，别挂面板 |
| 点格子没反应 | 格子预制体没挂 Button 组件 | 给 WeaponSlot 预制体加 Button |
| 装备不显示模型 | WeaponData 里 `weaponModel` 没拖模型 | 在武器 .asset 里拖上模型 |
| 控制台报「找不到 WeaponManager」 | `weaponManager` 字段断了 | 把 Hero 上的 WeaponManager 拖进 InventoryManager |
| 已装备的不变金色 | 格子的 `background` 字段没拖 | 把格子背景 Image 拖进 WeaponSlot 的 background |

## 七、两个建议（在 Inspector 里改，不动代码）

1. 挂 InventoryManager 的空物体现在叫 `WeaponManager`，跟 Hero 上的武器组件重名，容易混。建议改叫 `InventorySystem`。
2. 格子预制体里「名字」和「伤害」两个文本现在重叠在一起，可以在编辑器里把伤害文本往下挪一点（或改小字体）。
