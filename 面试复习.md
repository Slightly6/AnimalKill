# AnimalKill 项目面试复习手册

## 一、项目一句话介绍（开场白）

> 这是一款 **动物扑克 + 德州扑克 + roguelike** 的卡牌游戏，Unity 开发，目标上架 Steam。52 关（4 花色 × A~K），肉鸽式地图探索（类似杀戮尖塔），战斗用德州扑克牌型结算，战利品区凑 5 张牌比大小。

## 二、整体架构（面试讲主线）

一套主线贯穿全项目，能一句话讲清：

> **泛型单例 `Singleton<T>` + 静态事件总线 `EventBus` + 静态类 `GameProgress` 做跨场景存档**

分四层：

```
数据层    ScriptableObject（LevelDatabase / CardDataSO）+ [Serializable] POCO（LevelConfig / MapNodeData）
逻辑层    纯函数算法（PokerHandEvaluator / CardAnimator）+ Manager 单例（Battle/Game/Deck/Map...）
表现层    UI 订阅 EventBus 事件刷新 + 协程动画（ChipStack / Card / FadeManager）
交互层    鼠标事件 + 数学平面射线检测（CardDisplay / 各 OnMouseDown）
```

**数据流**：数据变化 → `EventBus.Publish` → 各 UI `Subscribe` 自动刷新，模块之间不互相持有引用。

## 三、核心考点（可背，按面试频率排序）

### 1. 泛型单例 `Singleton<T>` ⭐⭐⭐⭐⭐
```csharp
public class Singleton<T> : MonoBehaviour where T : MonoBehaviour
```
- **三级兜底**：`Instance` getter 里先 `FindObjectOfType<T>()` → 找不到 `new GameObject + AddComponent<T>()`。
- **防重复**：`Awake` 里 `if (_instance != null && _instance != this) Destroy(gameObject)`。
- **注意点**：本类**没做 `DontDestroyOnLoad`**——跨场景数据交给静态类 `GameProgress`，跨场景的物体（AudioManager/PauseMenu）在自己 `Awake` 里手动 `DontDestroyOnLoad`。
- **子类重写**：`protected override void Awake()` 里先 `base.Awake()`（赋值 Instance + 判重），再 `if (Instance != this) return;` 做自己的初始化。

### 2. 事件总线 `EventBus`（观察者/发布-订阅）⭐⭐⭐⭐⭐
```csharp
static Dictionary<Type, Delegate> handlers;   // 一个字典存所有事件
Subscribe<T>(Action<T>) / Unsubscribe<T> / Publish<T>
```
- 用 `typeof(T)` 作 key 按事件类型路由，`Delegate.Combine/Remove` 做多播。
- **事件载荷用 `struct`**（`where T : struct`）减少堆分配。
- **订阅必须退订**：`Start` 订阅、`OnDestroy` 退订，否则切场景后空引用/重复触发/内存泄漏——**这是最高频面试追问点**。
- `Clear()` 切场景清空。
- 12 种事件：`PhaseChangedEvent / CardPlayedEvent / CardDiedEvent / ChipsChangedEvent / TrophyChangedEvent / LevelClearedEvent` 等。

### 3. 协程（Coroutine）⭐⭐⭐⭐⭐
全项目最核心的流程控制手段：
- `IEnumerator + yield return null`（等一帧）、`yield return new WaitForSeconds`、`yield return StartCoroutine(...)`（嵌套等子协程完成）。
- **典型用法**：BattleManager 用协程写回合状态机；FadeManager 用嵌套协程串"变黑→切场景→变亮"；Chest 用协程串"开盖→抽卡→三选一"。
- **注意**：`WaitForSeconds` 受 `Time.timeScale` 影响，但 `OnGUI` 和 `WaitForSecondsRealtime` 不受。

### 4. 跨场景持久化 ⭐⭐⭐⭐⭐
两种方案对比（可讲取舍）：
| 方案 | 用法 | 适用 |
|------|------|------|
| 静态类 | `GameProgress`（纯数据） | 纯数据状态，易序列化、易测试 |
| DontDestroyOnLoad | AudioManager / PauseMenu / FadeManager | 需要存活的行为（播放/监听/画 UI） |

### 5. 场景切换 + 淡入淡出 `FadeManager` ⭐⭐⭐⭐
- **手写单例**（对比 Singleton 基类，展示两种单例写法）。
- 运行时用代码建全屏 UI：`new GameObject + AddComponent<Canvas/Image>`，`anchorMin/Max` 撑满屏幕，`raycastTarget = false` 不挡点击。
- `Mathf.Lerp` + `Time.deltaTime` 做 alpha 渐变。

### 6. ScriptableObject（数据驱动）⭐⭐⭐⭐
- `LevelDatabase`（52 关配置）、`CardDataSO`（卡牌数据）都是 SO 资产，`[CreateAssetMenu]` 生成。
- 好处：资产内存共享（多引用不复制）、可 Inspector 编辑、策划可配置、与逻辑分离。

### 7. 存档 `SaveManager`（JsonUtility + PlayerPrefs）⭐⭐⭐⭐
- `JsonUtility.ToJson/FromJson` 把进度序列化成字符串，存 `PlayerPrefs.SetString`。
- `[System.Serializable]` 是 JsonUtility 序列化必要条件。
- **枚举转 int 存**（JsonUtility 对 enum 支持不稳）。
- **浅拷贝陷阱**：`new List<MapNodeData>(GameProgress.map)` 拷贝容器，避免存档与运行时共享引用。
- 可讲：PlayerPrefs 不适合大/敏感数据，无加密易篡改。

### 8. Fisher-Yates 洗牌 ⭐⭐⭐⭐
`DeckManager.Shuffle<T>`（泛型方法）和 `BoardManager.ShuffledLanes`：
```csharp
for (int i = n-1; i > 0; i--) {
    int j = Random.Range(0, i+1);   // 注意 int 版上限「包含」
    交换 list[i] 和 list[j];
}
```
- O(n) 均匀打乱，经典面试题。

### 9. 牌型判定 `PokerHandEvaluator` ⭐⭐⭐⭐
- 纯函数静态类（不碰 Unity，可单测）。
- 步骤：判同花（遍历花色）→ 排序判顺子 → `count[14]` 计数数组统计张数 → 由高到低判定（StraightFlush → Four → FullHouse → Flush → Straight → TwoPair → OnePair → HighCard）。
- **规则取舍**：A=1 最小，只支持 A-2-3-4-5 最小顺、不支持 10-J-Q-K-A 大顺——可主动讲这个设计取舍。
- 排序用 O(n²) 冒泡（5 张无所谓），可提优化意识。

### 10. 程序化地图生成 `MapGenerator` ⭐⭐⭐⭐（算法难点，重点讲）
- 4 花色 × 13 必过关（K=Boss）自下而上，必过关之间随机插**分岔区**（深度 1~3 层，每层 2~3 叉）。
- **保底逻辑**：某段分岔区没有奖励关就强制把第一个改成 Upgrade，保证可达性。
- **连线算法**（核心）：必经点↔分岔层全连（发散/汇聚）；分岔层之间**两遍贪心**就近连——第一遍每个下游就近连一个上游，第二遍没连出去的上游就近连一个下游，保证**每个节点不孤立、不断链**。
- 三段式：`GenerateMapData`（纯数据）→ `BuildNodes`（实例化）→ `DrawConnections`（LineRenderer 连线），数据/表现分离。

### 11. 加权随机 ⭐⭐⭐
`Chest`：累计权重 + 随机数落区间；`PickWeighted` 用 `1f/GetPower()` 做权重（点数越大越难抽），配合 `RemoveAt` 实现**不放回抽样**。

### 12. UI 拖拽 `DragButton`（EventSystems）⭐⭐⭐
- 一个类同时实现 3 个接口：`IBeginDragHandler / IDragHandler / IEndDragHandler`。
- `RectTransformUtility.RectangleContainsScreenPoint` 命中检测。
- **坑**：`eventData.pressEventCamera` —— Canvas 是 Screen Space-Overlay 时为 null，World Space 才传相机。
- `offset = rect.position - eventData.position` 记按下点，避免拖动瞬间按钮"跳"到鼠标中心。

### 13. 相机 / viewport 归一化坐标 ⭐⭐⭐
- `Camera.ViewportToWorldPoint`：把手牌/HUD 锚定到屏幕固定位置，跨分辨率自适应。
- `Input.mousePosition.y / Screen.height` 得到 0~1 判断鼠标位置，与分辨率解耦。
- `LateUpdate` 里更新相机（在物体移动之后）。

### 14. 性能细节（体现功底，可主动提）⭐⭐⭐
- **缓存 `Camera.main`**：内部是 `FindGameObjectWithTag`，每帧调用开销大。
- **`sqrMagnitude` 比距离**：避免开平方（BoardManager.FindNearestPlayerSlot）。
- **`raycastTarget = false`**：全屏黑图不挡点击。
- **SortingGroup / sortingOrder**：解决 Z-fighting 和遮挡。
- 拖拽用**数学平面 `Plane(y=0)` 射线检测**替代碰撞体，减少碰撞开销。

## 四、设计模式清单（面试一句话各一个）

| 模式 | 在哪 |
|------|------|
| 单例 | `Singleton<T>` 基类 + 手写单例（FadeManager） |
| 观察者/发布-订阅 | `EventBus` + `UnityEvent`（DragButton）+ `sceneLoaded`（AudioManager） |
| 门面 | `MainMenu` / `FadeManager.Go` / `AudioManager.PlayXXX` |
| 状态机 | `BattleManager`（TurnPhase 枚举 + 协程）、`PauseMenu`（bool 切态） |
| 工厂 | `BoardManager.GenerateSlots` / `ScreenHUD.SpawnHud` |
| DTO/数据载体 | `SaveData` / `LevelConfig` / `MapNodeData` |
| 纯函数工具 | `PokerHandEvaluator` / `CardAnimator` |
| 贪心算法 | `ChipStack.GreedyBreak`、`MapGenerator` 连线 |

## 五、C# 语言点清单

- **泛型**：泛型类（`Singleton<T>`）、泛型方法（`Shuffle<T>`、`EventBus.Subscribe<T>`）、泛型约束（`where T : MonoBehaviour` / `where T : struct`）
- **委托/事件**：`Action<T>`、多播委托、`sceneLoaded` 事件订阅、方法组
- **属性**：自动属性 `{ get; private set; }`、计算属性（`IsEmpty`）、属性初始化器
- **枚举**：显式赋值（`Ace=1`）、强转 `(int)`、`NodeType`/`TurnPhase`/`HandType`/`CardSuit`/`CardRank`
- **集合**：`List<T>`、`Dictionary<Type, Delegate>`、`bool[]` 标记、`int[]` 计数数组
- **对象初始化器**：`new CardAttackedEvent { attacker = this }`
- **静态成员**：静态类、静态字段（`GameProgress` 全静态）、静态方法
- **协程**：`IEnumerator` + `yield` 全家桶
- **特性**：`[Serializable]`、`[Header]`、`[CreateAssetMenu]`、`[RequireComponent]`、`[NonSerialized]`
- **接口多实现**：`DragButton` 实现 3 个 EventSystems 接口

## 六、Unity API 清单（按分类）

- **生命周期**：Awake / Start / Update / LateUpdate / OnDestroy
- **协程**：StartCoroutine / StopCoroutine / IEnumerator / yield
- **场景**：SceneManager.LoadScene / sceneLoaded / GetActiveScene
- **持久化**：DontDestroyOnLoad / PlayerPrefs / JsonUtility
- **渲染**：SpriteRenderer / MeshRenderer / LineRenderer / SortingGroup / Canvas / Image
- **物理/输入**：BoxCollider(2D) / SphereCollider / isTrigger / OnMouseDown/Drag/Up
- **UI**：TMPro（TextMeshPro / TextMeshProUGUI）、Slider、UnityEvent、EventSystems 拖拽接口、OnGUI
- **数学**：Mathf.Lerp/Clamp/Abs/Sin/Pow、Quaternion（Euler/Slerp/AngleAxis）、Vector3（Lerp/Cross/sqrMagnitude）、Plane.Raycast、ViewportToWorldPoint
- **资源**：Resources.Load、ScriptableObject、Instantiate/Destroy/AddComponent/GetComponent

## 七、可主动讲的 5 个亮点（面试加分项）

1. **程序化肉鸽地图**：分层 DAG + 保底奖励关 + 两遍贪心连线保证连通性（断链处理）。
2. **事件总线解耦**：出牌→摆位→死亡→清槽→回合推进，整条链路靠事件解耦，模块零互相引用。
3. **数据/表现分离**：筹码真实值在 GameManager，ChipStack 只做视觉（贪心拆面值 + 正弦弧线飞行动画），死亡清槽靠事件反查。
4. **单例两种写法对比**：泛型基类 vs 手写，能讲清继承初始化顺序、防重复、DontDestroyOnLoad 取舍。
5. **性能意识**：缓存 Camera.main、sqrMagnitude、raycastTarget、数学平面替代碰撞体。

## 八、可能的追问 + 改进点（体现深度）

- **Q：EventBus 为什么 `where T : struct`？** 值类型事件减少 GC；代价是不能发 class 类型、无弱引用订阅者。
- **Q：`FindObjectOfType` 的性能？** 全场景遍历，频繁调用有开销，可换静态缓存/注册表。
- **Q：全局静态状态的问题？** 难以单测、作用域不可控、Reset 手动逐字段易漏。
- **Q：`drawPile.RemoveAt(0)` 复杂度？** O(n)（List 头部删除搬移），追求性能换 `Queue`，但洗牌需随机访问，这里换简洁。
- **Q：贪心找零一定最优吗？** 面值非"规范"时不一定，是经典可讨论点。
- **Q：PlayerPrefs 的缺点？** 不适合大/敏感数据，无加密易篡改，跨平台行为差异。
