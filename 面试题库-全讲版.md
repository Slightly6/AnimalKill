# Unity 秋招面试 · 全讲版

> 这份把你面试会考的**所有东西**都讲透了。每一条 = 是什么 + 例子 + 考试标准答案。
> 别贪多，按「C# → Unity → 算法 → 设计模式」的顺序看，用你自己的游戏（AnimalKill）当例子记忆。

---

# 第一部分：C# 基础（面试占比最高）

## 1. 值类型 vs 引用类型（必背）

**是什么**：变量存数据的方式有两种。

- **值类型**：数据直接存**栈**上，赋值 = **复制一份**
- **引用类型**：数据存**堆**上，栈里只存**地址**，赋值 = **传地址**（指向同一个）

**哪些是值类型**：`int` `float` `double` `bool` `char` `struct` `enum`
**哪些是引用类型**：`class` `string` `数组` `delegate` `object` `interface`

**例子**：
```csharp
int a = 10;
int b = a;   // 复制一份，b = 10
b = 20;      // a 还是 10 ✅

List<int> x = new List<int>{1};
List<int> y = x;   // y 和 x 指向同一个 List
y.Add(2);          // x 也变成 {1,2} ✅
```

**考试答案**：值类型在栈、赋值是复制；引用类型在堆、赋值是传地址。**`string` 是引用类型**（最容易答错）。

**记忆**：`struct` 是值类型，`class` 是引用类型。`string` 和数组看着像值，其实是引用。

---

## 2. 装箱 / 拆箱（必背）

**是什么**：
- **装箱**：值类型 → `object`（或接口），数据从栈**复制到堆**，慢
- **拆箱**：`object` → 值类型，强制转回来

**例子**：
```csharp
int n = 5;
object o = n;      // 装箱（慢）
int m = (int)o;    // 拆箱
```

**为什么慢**：多了一次堆分配 + 复制，还会引发 GC 卡顿。

**考试答案**：装箱 = 值类型转 object，在堆上复制一份，有开销；泛型 `List<T>` 就是为了避免装箱。

---

## 3. GC（垃圾回收）

**是什么**：C# 自动回收不再被引用的堆内存。

**原理**：托管堆分**三代**（0/1/2），新生对象在 0 代，GC 用**标记-清除**算法找没人引用的对象回收。GC 时会**卡顿**（stop the world）。

**Unity 里怎么优化**：
- 少 `new`、少字符串拼接（用 `StringBuilder`）
- 用**对象池**复用对象
- 避免装箱
- 能用 `struct` 就用 `struct`

**考试答案**：GC 自动回收堆内存，分代 + 标记清除，会卡顿，所以游戏里要少 new、用对象池。

---

## 4. struct vs class

| | struct | class |
|---|---|---|
| 类型 | 值类型 | 引用类型 |
| 内存 | 栈 | 堆 |
| 继承 | 不能继承（能实现接口） | 能继承 |
| 赋值 | 复制 | 传地址 |

**考试答案**：struct 是值类型、栈上、不能继承；class 是引用类型、堆上、能继承。

---

## 5. 委托、事件、泛型

**委托 `delegate`**：一个"指向方法的类型"，可以把方法当参数传。
```csharp
delegate int Op(int a, int b);
Op add = (a, b) => a + b;   // add(1,2) = 3
```

**事件 `event`**：受限的委托，外部只能 `+=` / `-=`（订阅/取消订阅），不能直接赋值或调用。
- **坑**：`+=` 订阅后忘记 `-=`，会导致**内存泄漏**（对象销毁了还被引用）。你的 `EventBus` 里 `OnDestroy` 里 `Unsubscribe` 就是防这个。

**内置泛型委托**：
- `Action`：无返回值
- `Func<T, TResult>`：有返回值
- `Predicate<T>`：返回 bool

**泛型**：编译时确定类型，**避免装箱、类型安全**。`List<int>`、`Dictionary<string, CardDataSO>` 都是。

**考试答案**：委托指向方法；事件是受限委托，+=/-= 订阅；泛型编译时定型、避免装箱。

---

## 6. 协程 vs 线程（必背）

**协程**：
- 跑在**主线程**上，**不是多线程**
- 用 `yield` 让出执行权，下一帧从这继续，**不阻塞**
- `yield return null` = 等一帧；`yield return new WaitForSeconds(1)` = 等 1 秒

**线程**：
- 操作系统级，**真并行**
- Unity 的 API 大多**只能在主线程调**（不能在线程里改 Transform）

**考试答案**：协程不是多线程，它在主线程上靠 yield 分段执行、不阻塞；线程才是真并行。

---

## 7. 接口 vs 抽象类

| | 接口 interface | 抽象类 abstract class |
|---|---|---|
| 方法 | 只有签名（无实现） | 可以有实现 |
| 字段 | 不能有 | 可以有 |
| 继承 | 一个类实现**多个** | 一个类继承**一个** |
| 语义 | "能做什么" | "是什么" |

**考试答案**：接口多实现、只有签名；抽象类单继承、可以有实现。

---

## 8. List / Dictionary 底层

**List**：底层是**数组**，满了会**扩容**（默认 4，翻倍），扩容要拷贝整个数组，所以能预估就 `new List<T>(capacity)`。

**Dictionary**：底层是**哈希表**，`key` 通过 `GetHashCode()` 算一个桶，冲突用链表解决。查找 **O(1)**。

**考试答案**：List 是数组、扩容有拷贝开销；Dictionary 是哈希表、查找 O(1)。

---

## 9. 多态、重载、重写

- **重载**：同名方法、参数列表不同（编译期决定）
- **重写**：`virtual` + `override`，子类覆盖父类方法（运行期决定）
- **多态**：父类引用指向子类对象，调用时走子类实现

**考试答案**：重载是编译期、同名不同参；重写是运行期、virtual+override；多态就是父类引用调子类方法。

---

## 10. 其他高频小点

- **`ref` vs `out`**：都传引用。`ref` 要先初始化、可读可写；`out` 方法内必须赋值、不用先初始化
- **`static`**：属于类本身，不用 new 就能用（你的 `GameProgress` 就是静态类）
- **`const` vs `readonly`**：`const` 编译期常量；`readonly` 运行期只读、可在构造函数里赋值
- **深拷贝 vs 浅拷贝**：浅拷贝只复制引用（改一个影响另一个）；深拷贝复制整个对象
- **`yield` 迭代器**：`IEnumerable`，懒加载，用一点算一点
- **反射**：运行期获取类型信息（`GetType()` / `typeof()` / `Activator.CreateInstance`）
- **`try/catch/finally`**：`finally` 一定执行
- **`StringBuilder`**：频繁拼字符串用它，比 `+` 快（少 GC）

---

# 第二部分：Unity 核心

## 1. 生命周期顺序（必背，一字不差）

```
Awake → OnEnable → Start → FixedUpdate → Update → LateUpdate → OnDisable → OnDestroy
```

分三组记：
- **创建时**：`Awake`（调一次，准备自己）→ `OnEnable`（每次激活）→ `Start`（第一帧前，找别人）
- **每帧**：`FixedUpdate`（固定 0.02s，物理）→ `Update`（每帧，逻辑）→ `LateUpdate`（Update 后，相机跟随）
- **结束时**：`OnDisable`（失活）→ `OnDestroy`（销毁）

**`Awake` vs `Start`**：`Awake` 只能准备自己（别处还没醒）；`Start` 时所有 `Awake` 都跑完了，可以安全找别人。

**你的例子**：`Card.Awake()` 里 `sortingGroup = GetComponent<SortingGroup>()`；`DeckManager.Start()` 里 `InitDeck()` + 订阅事件。

---

## 2. Update / FixedUpdate / LateUpdate

- `Update`：每帧，帧率不稳时次数不稳
- `FixedUpdate`：固定 0.02s，**物理**用它
- `LateUpdate`：一帧最后，**相机跟随**（等别人动完再跟）

---

## 3. 四元数 Quaternion（必背）

**是什么**：表示旋转的数学工具（四维），用来**避免欧拉角的"万向锁"**。

**常用**：
- `Quaternion.Euler(x, y, z)`：欧拉角 → 四元数
- `Quaternion.Lerp` / `Slerp`：旋转插值
- `transform.rotation`：世界旋转；`transform.localRotation`：本地旋转

**考试答案**：四元数避免万向锁，用 Euler 转换，Lerp/Slerp 插值。

---

## 4. 碰撞 vs 触发（必背）

| | 碰撞 Collision | 触发 Trigger |
|---|---|---|
| 方法 | `OnCollisionEnter/Stay/Exit` | `OnTriggerEnter/Stay/Exit` |
| 条件 | 都有 Collider，至少一方有 Rigidbody | 勾选 `isTrigger` |
| 效果 | 有物理阻挡 | 不阻挡，只通知 |

**考试答案**：碰撞用 OnCollision、要有 Rigidbody；触发勾 isTrigger、用 OnTrigger，只通知不阻挡。

---

## 5. 射线 Raycast

```csharp
RaycastHit hit;
if (Physics.Raycast(起点, 方向, out hit, 距离, layerMask)) { ... }
```
用途：鼠标点击拾取、视线判断、检测前方有没有东西。

---

## 6. Canvas 三种渲染模式

- **Overlay**：屏幕空间覆盖，永远最上层（UI 常用）
- **Camera**：屏幕空间但受相机影响
- **WorldSpace**：世界空间，3D 里的 UI（比如血条飘在角色头顶）

---

## 7. UI 适配

- **CanvasScaler**：`Scale With Screen Size` + 参考分辨率，不同屏幕等比缩放
- **锚点 Anchor**：UI 相对父节点定位，屏幕拉伸时跟着走

---

## 8. Animator 动画状态机

- `AnimatorController` + 状态 + 过渡
- 参数：`bool` / `int` / `float` / `Trigger`
- `SetTrigger("xx")` 切换状态
- 混合树：多个动画按参数混合

---

## 9. ScriptableObject（你在用）

**是什么**：一个数据容器，把数据存成 `.asset` 资源文件，不依赖场景。

**优点**：和场景解耦、可复用、省内存。

**你的例子**：`CardDataSO` 存卡牌数据（名字、战力、贴图），52 张卡都是 ScriptableObject。

---

## 10. Draw Call / 合批（性能必考）

- **Draw Call**：CPU 提交给 GPU 的一次绘制命令
- **为什么卡**：Draw Call 太多，CPU 忙不过来（CPU 瓶颈）
- **怎么减少**：
  - **静态批处理**：勾选 Static 的物体合并
  - **动态批处理**：顶点 < 900 的物体自动合（有严格条件）
  - **GPU Instancing**：同一网格大量实例
  - **图集**：多张贴图拼成一张，减少材质切换

**考试答案**：Draw Call 是 CPU 给 GPU 的绘制指令，多了 CPU 瓶颈；用合批/图集/Instancing 减少。

---

## 11. 资源加载方式

| 方式 | 特点 |
|---|---|
| `Resources.Load` | 简单，但都打进包、不能热更 |
| `AssetBundle` | 可分包、可热更、要自己管理 |
| `Addressables` | 官方推荐，异步加载 + 引用计数 |

---

## 12. 对象池（高频，能讲项目）

**是什么**：预先创建一批对象，用完**回收不销毁**，下次复用。

**为什么**：避免频繁 `Instantiate`/`Destroy` 造成 GC 卡顿。

**场景**：子弹、卡牌、敌人。

**面试能讲的**：你可以说"我的卡牌游戏里，宝箱开出的卡牌、战场上的卡牌，销毁时可以考虑对象池避免 GC"。

---

## 13. 其他高频小点

- **`Time.deltaTime`**：上一帧到这帧的时间，用它乘速度保证帧率无关（`transform.Translate(dir * speed * Time.deltaTime)`）
- **`NullReferenceException`**：空引用，最常见 bug
- **`Destroy` vs `DestroyImmediate`**：Destroy 延迟到帧末销毁；DestroyImmediate 立刻销毁（只在编辑器用）
- **`SortingGroup` / `SortingOrder`**：控制渲染顺序，越大越靠上
- **`Layer` / `LayerMask`**：分层，物理检测和渲染可以用
- **`Camera.main`**：主相机引用
- **`Instantiate` vs `AddComponent`**：复制已有对象 vs 给对象加脚本

---

# 第三部分：算法与数据结构（力扣）

## 必背代码模板

### 1. 二分查找
```csharp
int BinarySearch(int[] nums, int target)
{
    int left = 0, right = nums.Length - 1;
    while (left <= right)
    {
        int mid = left + (right - left) / 2;   // 防溢出写法
        if (nums[mid] == target) return mid;
        else if (nums[mid] < target) left = mid + 1;
        else right = mid - 1;
    }
    return -1;
}
```

### 2. 快排
```csharp
void QuickSort(int[] a, int left, int right)
{
    if (left >= right) return;
    int pivot = a[left];
    int i = left, j = right;
    while (i < j)
    {
        while (i < j && a[j] >= pivot) j--;
        a[i] = a[j];
        while (i < j && a[i] <= pivot) i++;
        a[j] = a[i];
    }
    a[i] = pivot;
    QuickSort(a, left, i - 1);
    QuickSort(a, i + 1, right);
}
```

### 3. 反转链表
```csharp
ListNode ReverseList(ListNode head)
{
    ListNode prev = null, cur = head;
    while (cur != null)
    {
        ListNode next = cur.next;
        cur.next = prev;
        prev = cur;
        cur = next;
    }
    return prev;
}
```

### 4. 二叉树遍历（递归）
```csharp
void PreOrder(TreeNode node)   // 前序：根左右
{
    if (node == null) return;
    Visit(node); PreOrder(node.left); PreOrder(node.right);
}
void InOrder(TreeNode node)    // 中序：左根右
{
    if (node == null) return;
    InOrder(node.left); Visit(node); InOrder(node.right);
}
void PostOrder(TreeNode node)  // 后序：左右根
{
    if (node == null) return;
    PostOrder(node.left); PostOrder(node.right); Visit(node);
}
```

### 5. BFS（层序遍历 / 走迷宫）
```csharp
// 用队列
Queue<T> q = new Queue<T>();
q.Enqueue(起点);
while (q.Count > 0)
{
    var cur = q.Dequeue();
    foreach (var next in 邻居(cur))
        q.Enqueue(next);
}
```

### 6. DFS（回溯）
```csharp
void Dfs(参数)
{
    if (终止条件) { 记录结果; return; }
    for (遍历选择)
    {
        做选择;
        Dfs(新参数);
        撤销选择;   // 回溯
    }
}
```

### 7. 两数之和（哈希）
```csharp
int[] TwoSum(int[] nums, int target)
{
    var map = new Dictionary<int, int>();
    for (int i = 0; i < nums.Length; i++)
    {
        int need = target - nums[i];
        if (map.ContainsKey(need)) return new[]{map[need], i};
        map[nums[i]] = i;
    }
    return null;
}
```

### 8. 有效括号（栈）
```csharp
bool IsValid(string s)
{
    var stack = new Stack<char>();
    foreach (char c in s)
    {
        if (c == '(') stack.Push(')');
        else if (c == '[') stack.Push(']');
        else if (c == '{') stack.Push('}');
        else if (stack.Count == 0 || stack.Pop() != c) return false;
    }
    return stack.Count == 0;
}
```

---

## 力扣经典题号（按题型）

| 题型 | 题号 | 名字 |
|---|---|---|
| 哈希 | 1 | 两数之和 |
| 哈希 | 217 / 242 / 49 | 存在重复 / 字母异位词 / 异位词分组 |
| 双指针 | 283 / 11 / 15 | 移动零 / 盛最多水的容器 / 三数之和 |
| 滑动窗口 | 3 | 无重复字符的最长子串 |
| 栈 | 20 / 155 | 有效括号 / 最小栈 |
| 链表 | 206 / 21 / 141 / 160 | 反转 / 合并两个有序 / 环形 / 相交 |
| 二分 | 704 / 35 / 69 / 33 | 二分查找 / 搜索插入位置 / x平方根 / 搜索旋转 |
| 树 | 94 / 144 / 145 / 102 / 104 / 226 / 110 | 前中后序 / 层序 / 最大深度 / 翻转 / 平衡 |
| DP | 70 / 121 / 198 / 53 / 300 | 爬楼梯 / 买卖股票 / 打家劫舍 / 最大子数组 / 最长递增子序列 |
| 回溯 | 46 / 78 / 22 / 39 | 全排列 / 子集 / 括号生成 / 组合总和 |
| 图 | 200 / 207 | 岛屿数量 / 课程表 |

**优先级**：哈希、双指针、栈、链表、二分、树遍历 > DP 简单题 > 回溯、图。

---

# 第四部分：设计模式（结合你的项目）

| 模式 | 一句话 | 你的例子 |
|---|---|---|
| **单例** | 一个类全局只有一个实例 | `Singleton<T>`、`DeckManager.Instance` |
| **观察者** | 发布-订阅解耦 | `EventBus` |
| **对象池** | 复用对象少 GC | 卡牌/子弹 |
| **状态机** | 状态切换 | 战斗回合阶段 |
| **工厂** | 统一创建对象 | 宝箱随机生成卡牌 |

**单例优缺点**：
- 优点：全局访问方便、只创建一次
- 缺点：全局耦合、不好测试、线程安全要处理

---

# 第五部分：项目深挖（提前准备好口述）

1. **项目架构**：`Singleton` + `EventBus` + `ScriptableObject`（CardDataSO）+ `GameProgress` 静态类跨场景存档
2. **最难/印象最深的 bug**：卡牌正反面 —— Quad 默认面朝 -Z，但 `SetFaceDown` 把 180° 当成背面，导致翻面相反。这题能讲 5 分钟，是加分项。
3. **为什么用 ScriptableObject**：和场景解耦、可视化配置、省内存，52 张卡一张一个 asset
4. **性能优化**：对象池、Draw Call 合批、少 GC
5. **存档**：`GameProgress` 静态类 + 跨场景继承

---

# 第六部分：计算机基础（会一点就够）

- **操作系统**：进程 vs 线程（进程是资源分配单位、线程是执行单位）、堆和栈区别
- **网络**：TCP（可靠、三次握手）vs UDP（快、不可靠）、HTTP 状态码 200/404/500
- **图形学**：渲染管线（顶点着色器 → 光栅化 → 片元着色器）、Draw Call、坐标系（世界/屏幕/相机）

---

# 最后：10 天怎么用这份资料

1. **D1-D3**：背第一部分 C# + 第二部分 Unity 的必背题（生命周期、值/引用类型、装箱、协程、四元数、碰撞触发、Draw Call、单例）
2. **D4-D6**：第三部分算法，把 8 个模板默写会，再刷题号表里的简单题
3. **D7-D8**：第五部分项目深挖，把 5 个问题练到能说
4. **D9-D10**：反复过必背题 + 背自我介绍

每天先默写一遍「生命周期顺序」和「值类型 vs 引用类型」，这两个答错基本就凉了。
